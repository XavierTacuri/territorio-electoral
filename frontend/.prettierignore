dist
node_modules
playwright-report
test-results

src/api/generated

# Generated by app.scripts.export_openapi (Python json.dumps); its exact byte
# format is diffed against the backend export in CI, never Prettier's.
openapi.json

# npm-managed lockfile; its format is npm's concern, not Prettier's.
package-lock.json
