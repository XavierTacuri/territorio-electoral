# Arquitectura de producción AWS — target (Fase 4C)

Este documento describe el **destino** de la infraestructura AWS para Territorio Electoral. Fase 4A preparó la _aplicación_ para poder desplegarse así — introdujo `ARTIFACT_STORAGE_PROVIDER=s3`, la subida directa de evidencia a S3 y las credenciales vía IAM. Fase 4B probó la _resiliencia_ de esa aplicación en una topología multiinstancia real (ver sección propia más abajo), sin crear ningún recurso AWS. **Ningún recurso de este documento existe todavía** — todos los identificadores (ARNs, nombres de bucket, dominios) son placeholders.

Fase 4C provisiona los recursos descritos aquí (Terraform) y mide carga real para decidir si Redis/SQS/RDS Proxy hacen falta — no se anticipan sin evidencia (§51 Fase 4A).

## Diagrama de flujo objetivo

```
Internet
   │
   ▼
CloudFront + WAF  ──────────────► S3 (frontend estático, build de Vite)
   │
   │ /api/*
   ▼
ALB (HTTPS, certificado ACM)
   │
   ▼
ECS/Fargate — servicio "api" (>= 2 tasks, Auto Scaling)
   │                                   │
   │ SQL (RDS Proxy)                   │ S3 API (SDK boto3, vía IAM Task Role)
   ▼                                   ▼
RDS Proxy ──► RDS PostgreSQL/PostGIS   S3 privado (evidencia, actas, informes)
   (Multi-AZ)                             │
                                           │ GET/POST presigned
                                           ▼
                                      Navegador / PWA del Delegado

Secrets Manager ──► credenciales DB/JWT/HMAC inyectadas como variables de entorno del task
CloudWatch ──► logs (stdout JSON de cada task), métricas, alarms
```

`FastAPI`/navegador hablan con S3 de dos formas, ambas ya implementadas en Fase 4A:

- **Server-side** (`app/services/artifact_storage.py::S3ArtifactStorage`): la API escribe informes generados y evidencia subida vía proxy (`ActivityEvidence`, `ElectionDayDocument`) directamente al bucket con `put_object`, y autoriza descargas devolviendo un redirect 307 a una URL GET firmada de corta duración — nunca transporta los bytes.
- **Cliente directo** (actas electorales únicamente): el Delegado sube la fotografía del acta directo desde el navegador al bucket vía un POST firmado (`presigned POST`) que la API autoriza pero nunca intermedia — ver `docs del flujo` en `app/services/election_act_service.py::create_upload_intent`/`complete_upload`.

## Componentes (todos placeholder hasta Fase 4C)

| Componente                      | Rol                                                                                                 | Notas                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------- | --------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CloudFront + WAF                | CDN + reglas de bloqueo (rate limiting, IP reputation) delante del frontend y del ALB               | Certificado ACM, HSTS                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| S3 (frontend)                   | Build estático de `npm run build`                                                                   | Bucket separado del bucket de artifacts; público solo vía CloudFront (OAC), nunca directo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ALB                             | Balanceo HTTPS hacia las tasks `api`                                                                | Health check contra `/api/v1/ready` — la semántica liveness/readiness que ese health check necesita ya está implementada y probada (Fase 4B)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ECS/Fargate                     | Ejecuta el contenedor `api` (mismo `backend/Dockerfile` de hoy)                                     | ≥2 tasks; ninguna task comparte filesystem — por eso `ARTIFACT_STORAGE_PROVIDER=s3` es obligatorio aquí; el comportamiento sin estado que esto requiere ya se demostró con dos procesos reales en Fase 4B                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ECS release task                | Corre `alembic upgrade head` una sola vez antes de que el servicio escale a >1 task                 | Fase 4B reprodujo por qué esto es obligatorio, no opcional: al levantar por primera vez la topología de prueba de dos réplicas (`docker-compose.multi-instance.yml`), ambas ejecutaron el `CMD` por defecto del Dockerfile (`alembic upgrade head && exec uvicorn ...`) en paralelo contra la misma base y compitieron por crear `alembic_version` (`UniqueViolation`) — no es una hipótesis, se reprodujo. La corrección de la topología de prueba (una réplica migra, la otra espera `service_healthy` antes de arrancar) es un sustituto aceptable _solo para pruebas_; en ECS real, la migración debe ser una release task en su propio ciclo de vida, ejecutada exactamente una vez, antes de que el servicio escale — nunca embebida en el `CMD` de cada task. |
| RDS Proxy                       | Pooling de conexiones hacia RDS                                                                     | Fase 4C; hoy la app ya usa `pool_pre_ping=True` + `pool_recycle` configurable (`DB_POOL_RECYCLE_SECONDS`, Fase 4B) — pool propio de SQLAlchemy, sin asumir un proxy todavía. `WEB_CONCURRENCY × (DB_POOL_SIZE + DB_MAX_OVERFLOW) × réplicas` debe mantenerse por debajo del máximo de conexiones que soporte RDS/RDS Proxy cuando se dimensione en Fase 4C.                                                                                                                                                                                                                                                                                                                                                                                                          |
| RDS PostgreSQL/PostGIS Multi-AZ | Base de datos, única fuente de verdad de metadata (incluida la de cada artifact)                    | Igual esquema que hoy; Fase 4A no requirió migración                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| S3 (artifacts)                  | Bucket privado de evidencia/actas/informes                                                          | Ver detalle abajo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Secrets Manager                 | `SECRET_KEY`, `BROWSER_REFRESH_TOKEN_HMAC_SECRET`, `SURVEY_SUBMISSION_HMAC_SECRET`, credenciales DB | Inyectados como env vars del task, nunca en la imagen                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| CloudWatch                      | Logs (el JSON estructurado que ya emite `app.core.observability`), métricas, alarms                 | `request_id` por línea ya viaja en cada log hoy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

## Arquitectura del bucket S3 de artifacts

- **Privado**: Block Public Access activado en las 4 opciones, sin excepción. Ninguna ACL `public-read`, nunca.
- **Versioning**: activado — protege contra un `delete_object` erróneo; el ciclo de vida (ítem siguiente) gestiona el costo.
- **Encryption**: SSE-S3 (`AES256`) por defecto, o SSE-KMS con una CMK dedicada si el requisito de cumplimiento lo exige (`S3_SSE_MODE=aws:kms` + `S3_KMS_KEY_ID`). Bucket policy debe **denegar** cualquier `PutObject` sin el header de cifrado correspondiente.
- **Prefijos**: `evidence/`, `reports/` (configurables vía `S3_EVIDENCE_PREFIX`/`S3_REPORT_PREFIX`). Dentro de `evidence/`, la subida directa de actas usa `evidence/pending/` mientras el objeto no ha sido verificado por `/evidence/complete`, y `evidence/final/` una vez promovido — la promoción es un `copy_object` server-side seguido de `delete_object` del pendiente (nunca al revés).
- **Lifecycle (pendiente, Fase 4C)**: una regla de expiración sobre `evidence/pending/*` (por ejemplo, 24-48h) limpia objetos huérfanos de intents nunca completados — la separación de prefijo ya existe en Fase 4A precisamente para que esta regla sea trivial de añadir sin tocar código de aplicación.
- **Retention de reportes (pendiente, Fase 4C)**: una regla de expiración sobre `reports/*` alineada con `REPORT_ARTIFACT_RETENTION_DAYS` (hoy la retención la aplica la aplicación marcando `is_available=false`; una regla de bucket sería una segunda capa, no la fuente de verdad).
- **CORS**: exacto para el dominio de la app —
  ```json
  [
    {
      "AllowedOrigins": ["https://app.example.invalid"],
      "AllowedMethods": ["GET", "POST"],
      "AllowedHeaders": ["*"],
      "MaxAgeSeconds": 300
    }
  ]
  ```
  Sin `"*"` en `AllowedOrigins` nunca en producción.
- **IAM least privilege — ECS Task Role**: solo `s3:PutObject`, `s3:GetObject`, `s3:DeleteObject`, `s3:CopyObject`, `s3:HeadObject` sobre `arn:aws:s3:::<bucket>/evidence/*` y `arn:aws:s3:::<bucket>/reports/*` (placeholder — sustituir por el ARN real). Nunca `s3:*`, nunca `Resource: "*"`.
- **CloudTrail data events (opcional, Fase 4C+)**: si se habilita auditoría a nivel de objeto, filtrar por el bucket de artifacts; evaluar el costo contra el valor real antes de activarlo — no se activa por defecto.

## Lo que Fase 4A ya deja listo para este target — IMPLEMENTADO

- `ARTIFACT_STORAGE_PROVIDER=s3` + `AWS_REGION`/`S3_ARTIFACT_BUCKET`/prefijos/timeouts/SSE — toda la configuración de este documento tiene ya un `Settings` validado (`backend/app/core/config.py`) y una fábrica (`app/services/artifact_storage_factory.py`) que la aplica sin tocar el código de los servicios.
- Credenciales exclusivamente vía el credential provider chain estándar de boto3 (`app/services/s3_client.py`) — en ECS, la IAM Task Role de la tabla anterior es lo único que hace falta configurar; la aplicación no tiene ningún campo para una access key estática.
- El flujo de subida directa de actas (`upload-intent` → POST firmado → `complete`) ya implementa exactamente el prefijo `pending/`→`final/` que la regla de lifecycle de arriba necesita.
- Los cuatro endpoints de descarga de artifacts (evidencia de actividad, documento de jornada, evidencia de acta, informe) ya devuelven un redirect a URL firmada cuando `provider=s3`, nunca transportan el archivo por FastAPI.
- **Integridad real del objeto subido**: el POST firmado exige `x-amz-checksum-sha256` (el checksum aditivo nativo de S3, no metadata declarativa) — S3 calcula el hash de los bytes que realmente recibe y rechaza el upload si no coincide con lo declarado, antes de almacenar nada. `complete()` vuelve a leer ese checksum vía `HEAD ... ChecksumMode=ENABLED` y lo compara contra el token — nunca confía en un valor que el cliente pueda afirmar independientemente de los bytes que subió, y nunca descarga el objeto completo para re-hashearlo.
- **Estado de la evidencia sin columna nueva**: `ElectionActEvidence` no tiene (ni necesita) un estado `PENDING`/`READY` explícito — la fila solo se crea dentro de `complete()`, después de validar el checksum y promover `pending/`→`final/`. Un objeto en `evidence/pending/` nunca cuenta como evidencia (no hay fila que lo referencie), y `submit_revision` exige al menos una `ElectionActEvidence` persistida — por construcción, nunca puede enviarse una revisión respaldada solo por un objeto pendiente sin confirmar. No se agregó migración por este motivo.

## Alcance real de los tests "multi-instancia" de Fase 4A, y qué añadió Fase 4B

`test_artifact_storage_multi_instance.py` prueba que dos objetos `S3ArtifactStorage` construidos de forma completamente independiente (cliente boto3 propio, Stubber propio) pueden operar sobre el mismo `storage_key` sin compartir nada más que el nombre del bucket — es decir, que la arquitectura **no asume** filesystem compartido a nivel de la clase de storage. `test_election_act_evidence_race_postgres.py` va un paso más allá y prueba la resolución de la carrera de `complete()` contra PostgreSQL real (no SQLite), con dos conexiones/hilos genuinamente concurrentes.

Ninguno de los dos, por sí solo, era el multi-instancia real: eran evidencia de que el _diseño_ es compatible con esa topología, a nivel de una sola clase o de un solo recurso. Fase 4B cerró esa brecha con `docker-compose.multi-instance.yml` + `backend/scripts/verify_multi_instance.py`: dos procesos FastAPI completos, en contenedores separados, sirviendo tráfico HTTP real, ejercitados de punta a punta (login, jornada, soporte admin, actas, reclamos, validación, Centro de Control, idempotencia, invitaciones) — la prueba más cercana a la topología ECS/ALB de este documento que es posible hacer sin crear recursos AWS reales. Lo único que sigue faltando frente al target de esta página es el ALB, RDS Proxy y el auto-scaling reales — infraestructura, no comportamiento de la aplicación — que corresponde a Fase 4C.

## Lo que Fase 4B ya deja listo para este target — IMPLEMENTADO

Fase 4B no creó ningún recurso AWS ni Terraform — probó, con evidencia reproducible, que la _aplicación_ ya se comporta como esta arquitectura objetivo exige:

- **Statelessness real, no supuesta**: `docker-compose.multi-instance.yml` levanta dos procesos FastAPI independientes (`api-a`, `api-b`, contenedores separados) contra un único PostgreSQL compartido, y `backend/scripts/verify_multi_instance.py` ejercita en esa topología real: login cruzado (un token emitido por una instancia es aceptado por la otra sin volver a autenticar — prueba que no hay sesión local), estado de la Jornada idéntico desde ambas, una sesión de soporte admin iniciada en una instancia y reconocida de inmediato en la otra, un borrador de acta creado en una instancia y leído en la otra, un reclamo de revisión tomado en una instancia que produce un 409 coherente al intentarlo desde la otra, una validación hecha en una instancia reflejada exactamente una vez en el Centro de Control de la otra, un `client_generated_id` reintentado cruzando de instancia que nunca duplica acta/evidencia, y una invitación aceptada en una instancia reconocida de inmediato en la otra. Los ocho escenarios pasaron contra contenedores reales, no contra fakes en memoria.
- **El hallazgo real de esa prueba** (y su corrección) está documentado en la fila "ECS release task" de la tabla de arriba: dos réplicas migrando en paralelo compiten por `alembic_version`. La arquitectura ECS de este documento ya asume una release task separada por esta razón concreta, no por precaución genérica.
- **Inyección de fallos, verificada manualmente contra la topología real**: DB detenida → `/ready` responde 503 sin filtrar detalles de conexión/credenciales y sin que el proceso API se reinicie; DB restaurada → `/ready` vuelve a 200 solo, sin reiniciar el contenedor; una instancia detenida → la otra sigue sirviendo lecturas y escrituras (Centro de Control, login) sin degradación; instancia restaurada → vuelve a quedar `healthy` sola.
- **DB pooling listo para RDS/RDS Proxy**: `pool_pre_ping=True` (detecta una conexión ya muerta) + `pool_recycle` configurable vía `DB_POOL_RECYCLE_SECONDS` (recicla una conexión antes de que RDS/RDS Proxy la cierre silenciosamente por su cuenta) — ambos pasan a ser opciones reales de `Settings`, cubiertas por tests (`backend/tests/test_db_pool_config.py`) que además prueban que SQLite (toda la suite de tests) nunca recibe opciones exclusivas de Postgres.
- **`/health` (liveness) y `/ready` (readiness) con semántica correcta**: `/health` nunca toca la base de datos (para que ECS/ALB no reinicien un task sano solo porque la base está momentáneamente lenta); `/ready` sí la toca y refleja fielmente si el task puede servir tráfico.
- **Request ID sanitizado** de punta a punta (`X-Request-ID`, siempre devuelto, regenerado si el valor entrante es sospechoso) y logging JSON estructurado con **redacción centralizada** (`SensitiveDataFilter`, defensa en profundidad: nunca depende de que cada desarrollador recuerde no loguear un token) — listo para CloudWatch Logs Insights filtrando por `request_id`.
- **Métricas de bajo cardinality** (`territorio_events_total{event="..."}`) para eventos operativos de jornada/artifacts (envíos de acta, conflictos de reclamo/validación, fallas de subida) — sin IDs de usuario/acta/candidato como label, listas para un scraper CloudWatch/Prometheus sin explosión de series.
- **Graceful shutdown**: un `lifespan` de FastAPI libera el engine de SQLAlchemy después de que las peticiones en vuelo terminan — necesario para que ECS pueda drenar una task durante un despliegue sin cortar conexiones a mitad de una transacción.
- **`WEB_CONCURRENCY`** wireado en `Dockerfile` y `docker-compose.prod.yml` (shell-form `CMD`/`command`, para que se expanda desde el entorno del contenedor) — el número de workers uvicorn por task ya es una variable de despliegue, no un valor fijo en la imagen.
- **Política de reintentos del cliente offline** (`frontend/src/offline/syncEngine.ts`): backoff exponencial + jitter acotado ante fallas transitorias (red, 502/503/504, 429 con `Retry-After`), sin reintentar nunca 400/401/403/409/422 — para que un ALB momentáneamente saturado durante un despliegue no se traduzca en registros perdidos ni en una estampida de reintentos sincronizados de muchos dispositivos a la vez.

## Lo que Fase 4C debe hacer (no incluido en este ciclo)

- Terraform para cada recurso de la tabla anterior.
- La ECS release task en sí (Fase 4B probó _por qué_ hace falta y cómo se ve su ausencia; Fase 4C la implementa como recurso real).
- Las reglas de lifecycle de S3 descritas arriba.
- RDS Proxy real y la migración de `docker-compose.prod.yml`/Render hacia ECS.
- Terminación TLS real (ACM) y HSTS delante del ALB/CloudFront — hoy `frontend/nginx.conf` asume que esto lo añade un terminador externo, igual que en Render.
- Dimensionar `WEB_CONCURRENCY`/`DB_POOL_SIZE`/`DB_MAX_OVERFLOW`/número de tasks contra los límites reales de conexión de RDS/RDS Proxy — Fase 4B dejó la fórmula y las variables listas, no un valor de producción.

## Lo que Fase 4C decide, no antes

Redis, SQS, y cualquier infraestructura de colas/cache: el dominio ya usa constraints/locks de base de datos para la concurrencia crítica (por ejemplo, el `UNIQUE` de `(operation, board, contest)` en actas) — Fase 4B confirmó además que esos locks nunca retienen I/O externo (S3, generación de PDF) mientras están tomados. No se introduce infraestructura adicional por anticipación — primero se mide carga real contra el target de arriba.
